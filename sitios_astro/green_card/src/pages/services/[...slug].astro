---
import { getCollection, render } from "astro:content";
import Servicios de ReclutamientoLayout from "../../layouts/Servicios de ReclutamientoLayout.astro";
export async function getStaticPaths() {
  const work = await getCollection("services");
  const paths = work.map((page) => {
    return {
      params: { slug: page.id },
      props: { page },
      trailingSlash: false,
    };
  });
  return paths;
}
const { page } = Astro.props;
const { Content } = await render(page);
---
<Servicios de ReclutamientoLayout frontmatter={page.data}>
  <Content />
</Servicios de ReclutamientoLayout>
