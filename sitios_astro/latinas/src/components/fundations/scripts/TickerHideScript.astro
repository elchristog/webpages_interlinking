<!-- Show ticker only on index and /blog -->
<script type="module">
  const path = window.location.pathname;
  if (path !== "/" && path !== "/blog") {
    document
      .getElementById("tickerElement")
      ?.style.setProperty("display", "none");
  }
</script>
