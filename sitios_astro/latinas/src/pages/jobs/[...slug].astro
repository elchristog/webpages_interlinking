---
import { getCollection, render } from "astro:content";
import Ofertas LaboralesLayout from "@/layouts/Ofertas LaboralesLayout.astro";
export async function getStaticPaths() {
  const jobs = await getCollection("jobs");
  const paths = jobs.map((page) => {
    return {
      params: { slug: page.id },
      props: { page },
      trailingSlash: false,
    };
  });
  return paths;
}
const { page } = Astro.props;
const { Content } = await render(page);
---

<Ofertas LaboralesLayout frontmatter={page.data}>
  <Content />
</Ofertas LaboralesLayout>