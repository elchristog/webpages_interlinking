---
// Fundations
import Text from "@/components/fundations/elements/Text.astro";
import Button from "@/components/fundations/elements/Button.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";
import MobileNav from "@/components/global/MobileNav.astro";

const navLinks = [
  { label: "Customers", href: "/customers" },
  { label: "Blog", href: "/blog" },
  { label: "Help", href: "/helpcenter" },
  { label: "Jobs", href: "/jobs" },
  { label: "Inicio", href: "/system/overview" },
  { label: "Contacto Buio", href: "https://lexingtonthemes.com/templates/buio" },
];
---

<div
  id="nav-wrapper"
  class="mx-auto fixed top-0 w-full py-4 inset-x-0 z-100 transition-all duration-300 ease-in-out"
>
  <Wrapper class="standard">
    <div
      id="navigation-wrapper"
      class="relative flex items-center justify-between"
    >
      <!-- Logo -->
      <Text
        tag="a"
        variant="textXL"
        href="/"
        aria-label="Go to homepage"
        title="Homepage"
        class="text-white"
      >
        buio®
      </Text>

      <!-- Desktop Navigation -->
      <nav class="hidden md:flex items-center gap-4">
        {
          navLinks.map((link) => (
            <a
              title={link.label}
              aria-label={link.label}
              href={link.href}
              class="hover:text-base-300 text-white text-sm"
            >
              {link.label}
            </a>
          ))
        }
        <div class="flex items-center gap-1">
          <Button isLink size="sm" variant="muted" href="/sign-in">
            Asesoría
          </Button>
          <Button isLink size="sm" variant="default" href="/sign-up">
            Sing up
          </Button>
        </div>
      </nav>

      <!-- Mobile Navigation -->
      <MobileNav navLinks={navLinks} />
    </div>
  </Wrapper>
</div>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const navWrapper = document.getElementById("nav-wrapper");
    const scrollThreshold = 20;

    function handleScroll() {
      const scrolled = window.scrollY > scrollThreshold;

      navWrapper.classList.toggle("backdrop-blur-md", scrolled);
      navWrapper.classList.toggle("bg-base-950/80", scrolled);
      navWrapper.classList.toggle("py-2", scrolled);
      navWrapper.classList.toggle("py-4", !scrolled);
    }

    window.addEventListener("scroll", handleScroll);
    handleScroll();
  });
</script>
